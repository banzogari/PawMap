<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Category filter chips on the map/search screen (Figma pill style). -->
    <style name="Widget.PawMap.Chip.Filter" parent="Widget.Material3.Chip.Filter">
        <item name="chipBackgroundColor">@color/sel_chip_bg</item>
        <item name="chipStrokeColor">@color/sel_chip_stroke</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="chipCornerRadius">20dp</item>
        <item name="chipIconTint">@color/sel_chip_text</item>
        <item name="chipIconSize">18dp</item>
        <item name="android:textColor">@color/sel_chip_text</item>
        <item name="checkedIconVisible">false</item>
    </style>

    <style name="TextAppearance.PawMap.Tab" parent="TextAppearance.Design.Tab">
        <item name="fontFamily">@font/nps_font</item>
        <item name="android:textSize">13sp</item>
        <item name="textAllCaps">false</item>
    </style>

    <!-- Applies our accent color to the system MaterialDatePicker (range picker). -->
    <style name="ThemeOverlay.PawMap.DatePicker" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/text_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary</item>
    </style>

    <style name="PawMap.Calendar.Weekday" parent="">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:fontFamily">@font/nps_font</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">13sp</item>
    </style>
</resources>